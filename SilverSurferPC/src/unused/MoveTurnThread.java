package unused;

/*
 * package gui;
 * 
 * import simulator.pilot.AbstractPilot;
 * 
 * //Thread to move and turn real-time, only needed for manual testing with the
 * arrow-buttons (forward, left, right). public class MoveTurnThread extends
 * Thread {
 * 
 * private final AbstractPilot pilot; private final int length; private final
 * int angles; private boolean done = false;
 * 
 * public MoveTurnThread(final String str, final AbstractPilot pilot, final int
 * length, final int angles) { super(str); this.pilot = pilot; this.length =
 * length; this.angles = angles; }
 * 
 * @Override public void run() { if (length == 0) pilot.rotate(angles); else
 * pilot.travel(length); done = true; }
 * 
 * public boolean getDone() { return done; } }
 */