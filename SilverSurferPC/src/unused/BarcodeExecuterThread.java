package unused;

/*
 * package communication;
 * 
 * //Executes a barcode in a different thread. public class
 * BarcodeExecuterThread extends Thread {
 * 
 * private final Communicator communicator; private final int barcode;
 * 
 * public BarcodeExecuterThread(final String str, final Communicator
 * communicator, final int barcode) { super(str); this.barcode = barcode;
 * this.communicator = communicator; }
 * 
 * private void executeBarcode(final int barcode) { try { switch (barcode) {
 * default: break; } } catch (final Exception e) { System.out
 * .println("Error in BarcodeExecuterThread.executeBarcode(int barcode)!"); } }
 * 
 * @Override public void run() { executeBarcode(barcode);
 * communicator.setExecutingBarcodes(false); } }
 */